export enum EnrollmentExchangeName {
  CREATED = "enrollment.created.exchange",
  CANCELED = "enrollment.canceled.exchange",
}

export enum EnrollmentRoutingKey {
  CREATED = "enrollment.created",
  CANCELED = "enrollment.canceled",
}
